# Name of this example
TARGET = demo_tinygpu

# C source files for this example
SRCS = main.c

# Optional: extra user flags (leave empty; we pass UART0_SIM_MODE from the command line)
USER_FLAGS =

# Include the common NEORV32 make infrastructure
# Path is the same style used by other examples (adjust if your tree differs)
include ../../common/common.mk